# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Build output
/dist/
/bin/

# Sensitive / local data
*.hash

# rsrc.syso is intentionally tracked — it embeds the UAC manifest into the binary.
# Do NOT add *.syso here.
